import { OverviewRatingsPagination } from '../models/overview-ratings-pagination.interface';

export const PAGINATION_DEFAULTS: OverviewRatingsPagination = {
    paginate: true,
    page: 1,
    records: 10
}